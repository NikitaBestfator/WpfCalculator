<UserControl x:Class="NikBestCalculator.Views.StandardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="MinHeight" Value="35"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Дисплей -->
        <TextBox FontSize="28" FontWeight="Bold" HorizontalContentAlignment="Right" 
                 VerticalContentAlignment="Center" Height="50" Margin="0,0,0,5"
                 IsReadOnly="True" Text="{Binding Display}"/>

        <!-- ИСТОРИЯ -->
        <Border Grid.Row="1" Height="80" Margin="0,0,0,5"
                Background="#F5F5F5" BorderBrush="#CCC" BorderThickness="1" CornerRadius="4">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding History}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" Margin="5"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#E0E0E0" 
                                    CornerRadius="4" 
                                    Margin="3" 
                                    Padding="8,4"
                                    BorderBrush="#AAA" 
                                    BorderThickness="1"
                                    MouseDown="HistoryItem_MouseDown">
                                <TextBlock Text="{Binding}" FontSize="12" Foreground="#333"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Кнопки управления историей -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5,0,8">
            <Button Content="Очистить историю" Command="{Binding ClearHistoryCommand}" MinWidth="140" Height="28" Background="#E0E0E0" Padding="10,0"/>
            <TextBlock Text="(двойной клик по записи — удалить)" Foreground="Gray" FontSize="11" VerticalAlignment="Center" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Кнопки калькулятора -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>

            <Button Content="7" Grid.Row="0" Grid.Column="0" Command="{Binding NumberCommand}" CommandParameter="7"/>
            <Button Content="8" Grid.Row="0" Grid.Column="1" Command="{Binding NumberCommand}" CommandParameter="8"/>
            <Button Content="9" Grid.Row="0" Grid.Column="2" Command="{Binding NumberCommand}" CommandParameter="9"/>
            <Button Content="/" Grid.Row="0" Grid.Column="3" Command="{Binding OperationCommand}" CommandParameter="/" Background="#4CAF50" Foreground="White"/>

            <Button Content="4" Grid.Row="1" Grid.Column="0" Command="{Binding NumberCommand}" CommandParameter="4"/>
            <Button Content="5" Grid.Row="1" Grid.Column="1" Command="{Binding NumberCommand}" CommandParameter="5"/>
            <Button Content="6" Grid.Row="1" Grid.Column="2" Command="{Binding NumberCommand}" CommandParameter="6"/>
            <Button Content="*" Grid.Row="1" Grid.Column="3" Command="{Binding OperationCommand}" CommandParameter="*" Background="#4CAF50" Foreground="White"/>

            <Button Content="1" Grid.Row="2" Grid.Column="0" Command="{Binding NumberCommand}" CommandParameter="1"/>
            <Button Content="2" Grid.Row="2" Grid.Column="1" Command="{Binding NumberCommand}" CommandParameter="2"/>
            <Button Content="3" Grid.Row="2" Grid.Column="2" Command="{Binding NumberCommand}" CommandParameter="3"/>
            <Button Content="-" Grid.Row="2" Grid.Column="3" Command="{Binding OperationCommand}" CommandParameter="-" Background="#4CAF50" Foreground="White"/>

            <Button Content="0" Grid.Row="3" Grid.Column="0" Command="{Binding NumberCommand}" CommandParameter="0"/>
            <Button Content="," Grid.Row="3" Grid.Column="1" Command="{Binding DecimalCommand}"/>
            <Button Content="C" Grid.Row="3" Grid.Column="2" Command="{Binding ClearCommand}" Background="#E53935" Foreground="White"/>
            <Button Content="+" Grid.Row="3" Grid.Column="3" Command="{Binding OperationCommand}" CommandParameter="+" Background="#4CAF50" Foreground="White"/>

            <Button Content="=" Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="4" FontSize="20" FontWeight="Bold"
                    Command="{Binding EqualsCommand}" Background="#1E88E5" Foreground="White"/>
        </Grid>
    </Grid>
</UserControl>